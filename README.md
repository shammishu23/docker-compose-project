# Docker Compose Python App

Simple Python application containerized using Docker and managed with Docker Compose.

## Features
- Dockerfile-based build
- Docker Compose setup
- Persistent storage using Docker volumes
